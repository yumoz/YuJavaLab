<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mapper.AccountMapper">
    <resultMap id="AccountResultMap" type="com.example.entity.Account">
        <id column="id" property="id"/>
        <result column="account_no" property="accountNo"/>
        <result column="name" property="name"/>
        <result column="balance" property="balance"/>
    </resultMap>

    <select id="selectById" resultMap="AccountResultMap">
        SELECT id, account_no, name, balance
        FROM account
        WHERE id = #{id}
    </select>

    <!-- 原子扣减：WHERE 带余额条件，余额不足时影响行数为 0，防止并发超扣 -->
    <update id="debit">
        UPDATE account
        SET balance = balance - #{amount}
        WHERE id = #{id} AND balance >= #{amount}
    </update>

    <!-- 原子增额：并发转账到同一账户时结果仍正确 -->
    <update id="credit">
        UPDATE account
        SET balance = balance + #{amount}
        WHERE id = #{id}
    </update>
</mapper>
