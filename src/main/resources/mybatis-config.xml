<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
    <!-- 配置环境：可以配置多个环境，default 指定默认环境 -->
    <environments default="development">
        <environment id="development">
            <!-- 事务管理器：JDBC 事务（依赖数据库连接） -->
            <transactionManager type="JDBC"/>
            <!-- 数据源：POOLED 连接池 -->
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.cj.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/mybatis_demo?useSSL=false&amp;serverTimezone=UTC&amp;allowPublicKeyRetrieval=true"/>
                <property name="username" value="mybatis_user"/> <!-- 替换为你的 MySQL 用户名 -->
                <property name="password" value="MyBatis@123456"/> <!-- 替换为你的 MySQL 密码 -->
            </dataSource>
        </environment>
    </environments>

    <!-- 配置映射器：告诉 MyBatis 去哪里找 Mapper XML 文件 -->
    <mappers>
        <mapper resource="mapper/UserMapper.xml"/>
    </mappers>
</configuration>