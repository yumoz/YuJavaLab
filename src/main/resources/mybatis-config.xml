<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
    <settings>
        <!-- 显式指定日志实现为 SLF4J，便于在 logback 中查看动态 SQL/参数/缓存命中日志 -->
        <setting name="logImpl" value="SLF4J"/>
    </settings>

    <typeHandlers>
        <typeHandler handler="com.example.typehandler.UserTypeTypeHandler" javaType="com.example.entity.UserType"/>
    </typeHandlers>

    <plugins>
        <plugin interceptor="com.github.pagehelper.PageInterceptor">
            <property name="helperDialect" value="sqlite"/>
            <property name="reasonable" value="true"/>
        </plugin>
        <plugin interceptor="com.example.interceptor.CreateTimeInterceptor"/>
    </plugins>

    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="com.example.datasource.HikariDataSourceFactory">
                <property name="driverClassName" value="org.sqlite.JDBC"/>
                <!-- ${app.db.url} 由 SqlSessionFactoryUtil 在 build 时注入系统属性（默认 jdbc:sqlite:yujavalab.db） -->
                <property name="jdbcUrl" value="${app.db.url}"/>
                <property name="username" value=""/>
                <property name="password" value=""/>
                <property name="maximumPoolSize" value="5"/>
            </dataSource>
        </environment>
    </environments>

    <mappers>
        <mapper resource="mapper/UserMapper.xml"/>
        <mapper resource="mapper/OrderMapper.xml"/>
        <mapper resource="mapper/AccountMapper.xml"/>
    </mappers>
</configuration>
